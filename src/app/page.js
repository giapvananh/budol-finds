<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Budol Finds PH</title>

  <script src="https://cdn.tailwindcss.com"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;800;900&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: "Poppins", sans-serif;
    }

    .line-clamp-2 {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  </style>
</head>

<body class="bg-[#f6f7fb] text-black">
  <header class="sticky top-0 z-50 border-b bg-white/95 backdrop-blur-xl">
    <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-3">
      <div class="flex items-center gap-3">
        <div class="flex h-11 w-11 items-center justify-center rounded-2xl bg-gradient-to-br from-pink-500 to-orange-500 text-lg font-black text-white shadow">
          BF
        </div>

        <div>
          <h1 class="text-lg font-black leading-tight sm:text-2xl">
            Budol Finds PH
          </h1>
          <p class="text-[10px] font-bold uppercase text-neutral-500 sm:text-xs">
            Viral Finds • Trending Now 🔥
          </p>
        </div>
      </div>

      <a href="#products" class="rounded-full bg-black px-4 py-2 text-xs font-black text-white sm:px-5 sm:text-sm">
        Shop
      </a>
    </div>
  </header>

  <main>
    <section class="mx-auto max-w-7xl px-3 py-4 sm:px-6 sm:py-8">
      <div class="relative overflow-hidden rounded-[28px] bg-gradient-to-br from-[#2c211b] via-[#5b3f2f] to-[#171412] p-5 text-white shadow-xl sm:rounded-[40px] sm:p-10">
        <div class="absolute right-4 top-4 flex h-20 w-20 flex-col items-center justify-center rounded-full bg-orange-500 text-center font-black shadow-2xl sm:h-28 sm:w-28">
          <span class="text-[10px]">UP TO</span>
          <span class="text-2xl leading-none sm:text-4xl">80%</span>
          <span class="text-xs">OFF</span>
        </div>

        <div class="relative z-10 max-w-xl">
          <div class="inline-flex rounded-full bg-white/15 px-4 py-2 text-[10px] font-black backdrop-blur sm:text-xs">
            🔥 #1 TRENDING PICKS
          </div>

          <h2 class="mt-5 text-4xl font-black leading-[0.9] sm:text-6xl lg:text-7xl">
            SMART FINDS
          </h2>

          <h3 class="text-4xl font-black leading-[0.9] text-orange-400 sm:text-6xl lg:text-7xl">
            BETTER LIVING
          </h3>

          <p class="mt-5 max-w-md text-sm font-semibold text-white/85 sm:text-base">
            Top viral products for home, gadgets, beauty, kitchen and daily essentials.
          </p>

          <a href="#products" class="mt-6 inline-flex rounded-2xl bg-gradient-to-r from-pink-500 to-orange-500 px-7 py-4 text-sm font-black text-white shadow-xl sm:text-base">
            SHOP NOW ›
          </a>
        </div>

        <div class="mt-9 grid grid-cols-4 gap-3 sm:max-w-lg">
          <div class="flex aspect-square items-center justify-center rounded-3xl bg-white text-4xl shadow-xl sm:text-5xl">⌚</div>
          <div class="flex aspect-square items-center justify-center rounded-3xl bg-white text-4xl shadow-xl sm:text-5xl">🎧</div>
          <div class="flex aspect-square items-center justify-center rounded-3xl bg-white text-4xl shadow-xl sm:text-5xl">🍚</div>
          <div class="flex aspect-square items-center justify-center rounded-3xl bg-white text-4xl shadow-xl sm:text-5xl">📷</div>
        </div>
      </div>
    </section>

    <section class="mx-auto max-w-7xl px-3 sm:px-6">
      <div class="grid grid-cols-4 gap-2 rounded-3xl bg-white p-3 shadow-md sm:p-5">
        <div class="text-center">
          <div class="text-2xl sm:text-3xl">🚚</div>
          <div class="mt-1 text-[10px] font-black sm:text-sm">Free Ship</div>
        </div>

        <div class="text-center">
          <div class="text-2xl sm:text-3xl">🛡️</div>
          <div class="mt-1 text-[10px] font-black sm:text-sm">Authentic</div>
        </div>

        <div class="text-center">
          <div class="text-2xl sm:text-3xl">✅</div>
          <div class="mt-1 text-[10px] font-black sm:text-sm">Secure</div>
        </div>

        <div class="text-center">
          <div class="text-2xl sm:text-3xl">💵</div>
          <div class="mt-1 text-[10px] font-black sm:text-sm">COD</div>
        </div>
      </div>
    </section>

    <section class="mx-auto max-w-7xl px-3 py-5 sm:px-6">
      <div class="overflow-x-auto rounded-3xl bg-white p-4 shadow-md">
        <div class="flex min-w-max gap-4 sm:grid sm:min-w-0 sm:grid-cols-7">
          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">📷</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Smart Home</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🎧</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Gadgets</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🎮</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Gaming</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🧴</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Beauty</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🍳</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Kitchen</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🛋️</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">Home</div>
          </div>

          <div class="w-20 flex-shrink-0 text-center sm:w-auto">
            <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-orange-100 text-3xl">🟣</div>
            <div class="mt-2 text-[10px] font-bold sm:text-xs">All</div>
          </div>
        </div>
      </div>
    </section>

    <section id="products" class="mx-auto max-w-7xl px-3 py-4 sm:px-6">
      <div class="mb-4 flex items-center justify-between">
        <h2 class="text-xl font-black sm:text-3xl">
          🔥 Today's Best Deals
        </h2>

        <span class="text-xs font-black text-orange-600 sm:text-sm">
          View All ›
        </span>
      </div>

      <div id="productGrid" class="grid grid-cols-2 gap-3 sm:gap-5 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5"></div>

      <div id="loading" class="py-10 text-center text-sm font-bold text-neutral-500">
        Loading products...
      </div>
    </section>
  </main>

  <footer class="mt-10 border-t bg-white py-8 text-center text-xs font-semibold text-neutral-500 sm:text-sm">
    © Budol Finds PH — Viral Shopee Finds
  </footer>

  <script>
    const API_URL =
      "https://opensheet.elk.sh/1SpBNvD2CdhvnaIep3Ssputr-0EsH2pwPnrZWl0p1Ktw/Sheet1";

    const FALLBACK_IMAGE =
      "https://placehold.co/600x600/f5f5f5/111?text=Product";

    const productGrid = document.getElementById("productGrid");
    const loading = document.getElementById("loading");

    function escapeHtml(text) {
      return String(text || "")
        .replaceAll("&", "&amp;")
        .replaceAll("<", "&lt;")
        .replaceAll(">", "&gt;")
        .replaceAll('"', "&quot;")
        .replaceAll("'", "&#039;");
    }

    function renderProduct(item) {
      const title = item.title || "Product";
      const price = item.price || "";
      const oldPrice = item.old_price || "";
      const image = item.image || FALLBACK_IMAGE;
      const badge = item.badge || "HOT";
      const link = item.link || "#";

      return `
        <article class="overflow-hidden rounded-3xl bg-white shadow-md transition hover:-translate-y-1 hover:shadow-xl">
          <div class="relative aspect-square bg-neutral-100">
            <div class="absolute left-2 top-2 z-10 rounded-xl bg-orange-500 px-2 py-1 text-[9px] font-black text-white sm:left-3 sm:top-3 sm:text-[10px]">
              ${escapeHtml(badge)}
            </div>

            <img
              src="${escapeHtml(image)}"
              alt="${escapeHtml(title)}"
              loading="lazy"
              class="h-full w-full object-cover"
              onerror="this.src='${FALLBACK_IMAGE}'"
            />
          </div>

          <div class="p-3">
            <h3 class="line-clamp-2 min-h-[38px] text-xs font-black sm:text-sm">
              ${escapeHtml(title)}
            </h3>

            <div class="mt-2 text-[10px] font-bold text-neutral-500">
              ⭐ 4.9 • 12k sold
            </div>

            <div class="mt-2 flex items-end gap-2">
              <div class="text-lg font-black text-orange-600 sm:text-xl">
                ${escapeHtml(price)}
              </div>

              ${
                oldPrice
                  ? `<div class="pb-1 text-[10px] font-bold text-neutral-400 line-through">${escapeHtml(oldPrice)}</div>`
                  : ""
              }
            </div>

            <a
              href="${escapeHtml(link)}"
              target="_blank"
              rel="noopener noreferrer"
              class="mt-3 flex w-full items-center justify-center rounded-xl bg-gradient-to-r from-orange-500 to-red-500 py-3 text-xs font-black text-white sm:text-sm"
            >
              Buy Now 🛒
            </a>
          </div>
        </article>
      `;
    }

    async function loadProducts() {
      try {
        const response = await fetch(API_URL);
        const products = await response.json();

        loading.style.display = "none";

        if (!Array.isArray(products) || products.length === 0) {
          productGrid.innerHTML = `
            <div class="col-span-full rounded-3xl bg-white p-8 text-center font-bold text-neutral-500">
              No products found.
            </div>
          `;
          return;
        }

        productGrid.innerHTML = products.map(renderProduct).join("");
      } catch (error) {
        loading.style.display = "none";

        productGrid.innerHTML = `
          <div class="col-span-full rounded-3xl bg-white p-8 text-center font-bold text-red-500">
            Failed to load products.
          </div>
        `;
      }
    }

    loadProducts();
  </script>
</body>
</html>